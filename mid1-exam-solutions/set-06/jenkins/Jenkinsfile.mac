pipeline {
    agent any

    stages {
        stage('Generate Report') {
            steps {
                sh 'echo "Jenkins report generated successfully." > report.txt'
                echo 'Report generated inside workspace.'
            }
        }
        stage('Display Report') {
            steps {
                sh 'cat report.txt'
            }
        }
    }
}
